Mono pill for stacks, years and disciplines. Never clickable — use `Button variant="ghost"` if it needs to filter.

```jsx
<Tag>Three.js</Tag>
<Tag>GLSL</Tag>
<Tag accent>2026</Tag>
```

- At most one `accent` tag per group.
